from collections import defaultdictn = int(input()) list1 = list(map(int, input().split())) num_map = defaultdict(list)groups = []total_groups = nfor ind in range(n):    num_map[list1[ind]].append(ind+1) if len(num_map) < 3:    print(0)else:    total = min(len(num_map[1]), len(num_map[2]), len(num_map[3]))    print(total)         for i in range(total):        print(num_map[1][i], num_map[2][i], num_map[3][i])